<li>
  <%= gravatar_for user, size: Settings.avatar_index_page.size %>
  <%= link_to user.name, user %>
  <% if current_user.is_admin? && !user.compare_user(current_user) %>
    | <%= link_to t(".delete"), user, method: :delete,
      data: {confirm: t(".confirm")} %>
  <% end %>
</li>
